"""
Kyuti — the PySide6/Qt desktop interface for Pysaurus.

A native Qt6 desktop interface for managing video collections.

The name "Kyuti" reads as "cutie" → Qt, the framework it is built on. It was
renamed from "pyside6" so the interface no longer collides with the `PySide6`
package name (its sibling experimental frontend is `videroid`, built on `videre`).
"""
